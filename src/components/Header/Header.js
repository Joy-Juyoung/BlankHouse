import React from 'react';
import { HeaderContrainer } from './HeaderStyle';

const Header = () => {
  return (
    <HeaderContrainer>
      <div>Header</div>
    </HeaderContrainer>
  );
};

export default Header;
